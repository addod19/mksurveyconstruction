<div id="<%= dom_id loader %>">
  <tr>
    <td><%= loader.job_date %></td>
    <td><%= loader.name_of_client %></td>
    <td><%= loader.amount_charged %></td>
    <td><%= loader.fuel %></td>
    <td><%= loader.balance %></td>
  
  </tr>
</div>
